name: "Build Docker (multi-arch): p11-kit SSH sidecar"

on:
  workflow_dispatch:
    inputs:
      release_version:
        type: string
        description: "Release version for the Docker image (example: 2.0.0)"

run-name: >-
  ${{ format('Build Docker (multi-arch): p11-kit SSH sidecar: {0}', github.event.inputs.release_version) }}

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push multi-architecture image
        uses: docker/build-push-action@v6
        with:
          context: ci/softhsm
          file: ci/softhsm/proxy.dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/lamassuiot/p11-kit-ssh-sidecar:latest
            ghcr.io/lamassuiot/p11-kit-ssh-sidecar:${{ github.event.inputs.release_version }}
