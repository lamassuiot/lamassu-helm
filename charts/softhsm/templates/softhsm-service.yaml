apiVersion: v1
kind: Service
metadata:
  name: {{ include "soft.fullname" . }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.ssh.port }}
    targetPort: 22
    protocol: TCP
    name: "ssh"
  selector:
    app: {{ include "soft.fullname" . }}
