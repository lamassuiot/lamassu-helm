{{- $svc := include "lamassu.svc.merged" (dict "root" $ "svcKey" "deviceManager") | fromYaml -}}
{{- if eq (include "lamassu.pdb.enabled" $svc) "true" }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "lamassu.componentName" (dict "root" $ "component" "device-manager") }}
  {{- include "lamassu.resourceMetadata" (dict "root" $ "component" "device-manager") | nindent 2 }}
spec:
  minAvailable: {{ $svc.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "lamassu.selectorLabels" (dict "root" $ "component" "device-manager") | nindent 6 }}
{{- end }}
