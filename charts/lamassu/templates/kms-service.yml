apiVersion: v1
kind: Service
metadata:
  name: kms
  namespace: {{.Release.Namespace}}
spec:
  selector:
    app: kms
  type: ClusterIP
  ports:
  - name: http
    port: 8085
    targetPort: 8085
    protocol: TCP
