{{- if .Values.services.va.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: va
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: va
  minReplicas: {{ .Values.services.va.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.va.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.services.va.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.va.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.services.va.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.va.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
