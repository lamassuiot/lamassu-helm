{{- if .Values.services.ui.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ui
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ui
  minReplicas: {{ .Values.services.ui.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.ui.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.services.ui.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.ui.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.services.ui.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.ui.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
