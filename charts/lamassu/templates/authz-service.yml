apiVersion: v1
kind: Service
metadata:
  name: authz
  namespace: {{.Release.Namespace}}
spec:
  selector:
    app: authz
  type: ClusterIP
  ports:
  - name: http
    port: 8085
    targetPort: 8085
    protocol: TCP
