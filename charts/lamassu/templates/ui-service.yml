apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: {{.Release.Namespace}}
spec:
  selector:
    app: ui
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP